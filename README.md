# ZozScript
Hello , ZozScript is a language which then get translated to JS in this repository I will  share the files you need to write ZozScript in html or standalone in notepad 
NOTE : don't use installing_zoz_to_run_standalone.html OR HTML_ACCESS_ZS.js they both are not needed
